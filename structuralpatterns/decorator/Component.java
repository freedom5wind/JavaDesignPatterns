package decorator;

/**
 * - defines the interface for objects that can have responsibilities added to
 * them dynamically.
 * 
 * @author freedom5wind
 *
 */
public interface Component {
	void operation();
}
